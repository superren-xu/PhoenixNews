<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_detail"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="qianfeng.com.phoenixnews.shownews.view.DetailActivity">

    <RelativeLayout
        android:id="@+id/detail_bottomBar"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_alignParentBottom="true">

        <ImageView
            android:id="@+id/iv_back"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:padding="12dp"
            android:src="@drawable/detail_bottom_back_icon"/>

        <ImageView
            android:id="@+id/iv_share"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_alignParentRight="true"
            android:padding="12dp"
            android:src="@drawable/detail_bottom_share_icon"/>

        <ImageView
            android:id="@+id/iv_collection"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_toLeftOf="@id/iv_share"
            android:padding="12dp"
            android:src="@drawable/detail_bottom_collection_icon"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginLeft="10dp"
            android:layout_marginRight="10dp"
            android:layout_toLeftOf="@id/iv_collection"
            android:layout_toRightOf="@id/iv_back"
            android:gravity="center"
            android:hint="写评论"
            android:textSize="18sp"/>
    </RelativeLayout>

    <WebView
        android:id="@+id/webview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/detail_bottomBar"
        android:layout_marginBottom="1dp"></WebView>
</RelativeLayout>
